scenario: "2op.yaml"
patch: "2op.yaml"

oneclient:
  oneclient-1:
    clients:
      client11:
        provider: "oneprovider-1"
        mode: force-directio
        default timeout: 40
  oneclient-2:
    clients:
      client21:
        provider: "oneprovider-2"
        mode: force-directio
        default timeout: 40

