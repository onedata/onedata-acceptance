# Name of the scenario to run
scenario: "scenario-1oz-2op"

spaces: false

onezone:
  batchConfig:
    createUsers: false

oneprovider-1:
  batchConfig:
    createUsers: false
  workerOverlayConfig: |-
    [
      {op_worker, [
          {rtransfer_block_size, 1}
      ]}
    ].

oneprovider-2:
  batchConfig:
    createUsers: false
  workerOverlayConfig: |-
    [
      {op_worker, [
          {rtransfer_block_size, 1}
      ]}
    ].