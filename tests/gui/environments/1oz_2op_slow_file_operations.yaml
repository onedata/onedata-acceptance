# Name of the scenario to run
scenario: "scenario-1oz-2op"

# Determines if spaces should be created. It can be either boolean value or
# list of spaces that should be created.
spaces: false

oneprovider-1:
  workerOverlayConfig: |- 
    [
      {op_worker, [
          {rename_file_chunk_size, 512}
      ]}
    ].

oneprovider-2:
  workerOverlayConfig: |-
    [
      {op_worker, [
          {rename_file_chunk_size, 512}
      ]}
    ].
    
